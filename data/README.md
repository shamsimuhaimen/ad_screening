# data
Folder to download and process datasets.
- `download`: staging area that contains original downloaded files.
- `raw`: contains data extracted from `download` folder. Scripts should read data from here.
- `processed`: contains model ready data generated by scripts.

Datasets to download are defined in `src/scripts/download_data.py`.