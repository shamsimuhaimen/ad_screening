[tool.black]
line-length = 120

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "screen-vision"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]
